USE WHEN: deciding where new code belongs, understanding the project's purpose, or planning a feature.

A Node.js CLI tool for testing the context-specs harness.

Current state: greenfield — no source files, no package.json, no build system yet.
The first feature will establish the structure; plan against this, not against a
structure that doesn't exist.

Decided:
- Runtime: Node.js
- Deliverable: a CLI tool (invoked from the terminal)

Not yet decided: framework, test runner, package manager, entry point path.
